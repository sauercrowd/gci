{
  "name": "gci-docs",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "docs:dev": "vitepress dev docs --host --port 5173",
    "docs:build": "vitepress build docs && mv docs/.vitepress/dist/* .",
    "docs:preview": "vitepress preview docs --host --port 4173"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
