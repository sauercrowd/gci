# `gci doctor`

Validate local configuration and remote deploy prerequisites.

## Usage

```bash
gci doctor [config_file]
```

## Useful flags

- `--server <name>`: override server from config.

## Example

```bash
gci doctor
gci doctor --server prod
```
