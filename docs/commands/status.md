# `gci status`

Show remote status for the deployed service stacks.

## Usage

```bash
gci status [config_file]
```

## Useful flags

- `--server <name>`: override server from config.

## Example

```bash
gci status
gci status --server prod
```
